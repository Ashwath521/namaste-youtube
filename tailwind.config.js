/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.js"],
  // multiple type file do this configuration in content
  // ["./src/**/*.{js,jsx}"]
  // single type file do this configuration in content
  // ["./src/**/*.js"]
  theme: {
    extend: {},
  },
  plugins: [],
};
